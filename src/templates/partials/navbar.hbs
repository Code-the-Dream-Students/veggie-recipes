<div class="container">
    <!-----navbar---->
    <div class="topnav">
      <div class="navbar navbar-expand-lg">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li>
            <a href="javascript:void(0);" class="icon" onclick="myFunction()">
              <i class="fa fa-bars"></i>
            </a>
          </li>
          <li>

          </li>
        </ul>
        <div class="logo-text" onClick="location.href='/';">
          <span>Vege</span>loper<small>&copy;</small>
        </div>
        {{#if loggedIn}}
        
        <div class="dropdown">
          <button type="button" class="btn dropdown-toggle" data-toggle="dropdown" aria-haspopup="true"
            aria-expanded="false">
            Account <i class="far fa-user"></i></button>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
            <a class="padding dropdown-item" href="/home">Your Recipes</a>
            {{!-- <a class="dropdown-item" data-toggle="modal" data-target="#updateModal">Update Profile</a> --}}
            {{!-- <button class="dropdown-item" href="/home">Your Recipes</button> --}}
            <button class="dropdown-item" data-toggle="modal" data-target="#updateModal">Update Profile</button>
            <form method="POST" action="/logout">
              <button class="dropdown-item">Logout</button>
            </form>
          </div>
        </div>
      </div>

<!-- Navigation links (hidden by default) -->
      <div id="myLinks">
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
      </div>
      </div>

        {{else}}
        <a href="/logIn" class="signUp login d-none d-md-block" data-toggle="modal" data-target="#logInModal">Login <i class="far fa-user"></i></a>
        <button class="btn signup" type="submit" data-toggle="modal" data-target="#signUpModal">Sign Up</button>
        {{/if}}
      </div>

      <!-- Navigation links (hidden by default) -->
      <div id="myLinks">
        <a href="#logIn" class="signUp d-block d-md-none" data-toggle="modal" data-target="#logInModal">Log in
          <i class="far fa-user"></i></a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
      </div>
    </div>
    <!-----/navbar---->
  </div>